version: '3'

services:

  server:
    container_name: plotter-server
    build: .
    ports:
      - 8080:8080
    environment:
      - PORT=8080
    working_dir:
      /PlotterServer
    volumes:
      - ./:/PlotterServer
    command: tail -f /dev/null
